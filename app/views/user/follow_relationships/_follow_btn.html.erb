<% if current_user.following?(user) %>
  <%= link_to "フォロー外す", user_follow_relationships_path(user.id), method: :delete %>
<% else %>
  <%= link_to "フォローする", user_follow_relationships_path(user.id), method: :post %>
<% end %>