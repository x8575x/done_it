<!--ログインしていて、いいねがされている場合-->
<% if user_signed_in? && book.read_by?(current_user) %>
  <%= link_to book_reads_path(book), method: :post, remote: true, class: "submit_create" do %>
    再読本に登録
  <% end %>
<!--ログインしていて、いいねがされていない場合-->
<% elsif user_signed_in? && !book.read_by?(current_user) %>
  <%= link_to book_reads_path(book), method: :post, class: "edit" do %>
    読んだ本に登録
  <% end %>
<!--ログインしていない場合-->
<% else %>
<% end %>